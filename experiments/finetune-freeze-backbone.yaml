%TAG !include! ./finetune.yaml
---
name: finetune-freeze-backbone
description: "Finetune the model with the backbone frozen"

checkpoint: original_strats

module_config:
  optimizer:
    trained_parameters:
      - head
