%TAG !include! ./ours.yaml
%TAG !depends-on! ../pretrain/ours.yaml
%TAG !data-fractions! 0.1,0.5,1.0
---

description: "Finetune our baseline model with weighted loss turned off"

module_checkpoint: ours_pretrain:best

module_config:
  weighted_loss: false
