%TAG !include! ./base_config.yaml
---

description: "Finetune the original model speedup with flash attention"

# FIXME: rename checkpoint

checkpoint: pretrain-ours-4:best

trainer:
  gradient_clip_val: 0.3

module_config:
  model:
    attention_dropout: 0.2

  optimizer:
    fused: false # Fused optimizer does not support gradient clipping
