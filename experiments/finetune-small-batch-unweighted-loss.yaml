%TAG !include! ./finetune.yaml
---
description: "Finetune on original Strats  with weighted loss turned off and small batch size"
name: finetune-unweighted-loss-small-batch

data_config:
  train:
    batch_size: 4

module_config:
  weighted_loss: false

