%TAG !include! ./pretrain-ecdf.yaml
---

description: "Pretrain the model with ECDF scaler and sigmoid activation function using unfiltered dataset"


data_config:
  train:
    path: data_with_outliers/train

  val:
    path: data_with_outliers/val

  test:
    path: data_with_outliers/test
