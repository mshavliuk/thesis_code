%TAG !include! ../finetune-base.yaml
%TAG !depends-on! ../pretrain/ecdf-uint8.yaml
---

description: "Finetune the model with ECDF scaler"

module_checkpoint: ecdf-uint8_pretrain:latest

module_config:
  model:
    quantized: true
    head_layers:
      - 'forecast_fc'
      - 'binary_fc'

#
#trainer:
#  accelerator: 'cpu'
