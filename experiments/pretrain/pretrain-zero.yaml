%TAG !include! pretrain-base.yaml
---

description: "Pretrain model with infinitely small learning rate for 1 epoch to create a pristine checkpoint"


trainer:
  max_epochs: 1

data_config:
  train:
    path: ./src/util/tests/data

module_config:
  optimizer:
    lr: 1.0e-100
    fused: true
