%TAG !include! ./finetune.yaml
---
name: Debug
description: "Debugging the finetuning process"

checkpoint: /home/user/.cache/thesis/dev-checkpoints/pretrain/strats-epoch=3-val_epoch_loss=0.66.ckpt
#checkpoint: Strats

#module_config:
#  optimizer:
#    trained_parameters:
#      - head

data_config:
  bootstrap: true

  train:
    balanced: true
    batch_size: 2
    path: ./src/util/tests/data

  val:
    batch_size: 2
    path: ./src/util/tests/data

  test:
    batch_size: 2
    path: ./src/util/tests/data

trainer:
  max_epochs: 5

checkpoint_callback:
  dirpath: /home/user/.cache/thesis/dev-checkpoints/finetune

early_stop_callback:
  patience: 3

results_dir: /home/user/.cache/thesis/dev-results
