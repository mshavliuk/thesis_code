%TAG !include! ./finetune.yaml
---
description: "Finetune on original Strats data and hyperparams, with class balancing via resampling"
name: finetune-class-balancing

module_config:
  weighted_loss: false

data_config:
  train:
    balanced: true
